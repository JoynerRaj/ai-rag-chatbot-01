version: "3.9"

services:

  fastapi:
    build: ./fastapi_service
    container_name: fastapi_service
    ports:
      - "8001:8001"

  django:
    build: ./django_chat
    container_name: django_chat
    ports:
      - "8000:8000"
    depends_on:
      - fastapi