# This file marks the django_chat project as a Python package.
# Project-level config lives here:
#
#   settings.py — Django settings (database, installed apps, middleware)
#   urls.py     — root URL configuration
#   asgi.py     — ASGI entry point for Daphne / WebSocket support
#   wsgi.py     — WSGI entry point (not used in production, kept for compatibility)
